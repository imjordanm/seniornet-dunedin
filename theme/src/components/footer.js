/** @jsx jsx */
import { jsx, Footer } from "theme-ui"

export const FooterWrapper = props => (
  <Footer>
    <div sx={{
      margin: "0 auto",
      maxWidth: "container",
      width: "default",
      display: "flex",
      justifyContent: "space-between"
    }}>
      Footer placeholder
    </div>
  </Footer>
)